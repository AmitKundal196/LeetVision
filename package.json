{
  "name": "leetvision",
  "version": "1.0.0",
  "description": "LeetVision AI Full Stack",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --include=dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "start": "npm start --prefix backend"
  }
}
